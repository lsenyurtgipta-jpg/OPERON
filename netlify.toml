# Vite + React (OPERON) — Netlify otomatik deploy ayarları
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA: tüm rotalar index.html (React tek sayfa)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
